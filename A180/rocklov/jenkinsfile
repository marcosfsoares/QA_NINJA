pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Configuration') {
            steps {
                sh 'cd A180 && cd rocklov && bundle install'
            }
        }
        stage('Testing') {
            steps {
                sh 'cd A180 && cd rocklov && cucumber'
            }
        }
    }
}
