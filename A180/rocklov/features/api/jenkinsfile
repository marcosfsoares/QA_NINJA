pipeline {
    agent {
        docker {
            image 'ruby'
        }
    }

    stages {
        stage('Configuration') {
            steps {
                sh 'bundle install'
            }
        }
        stage('Testing') {
            steps {
                sh 'cucumber'
            }
        }
    }
}
